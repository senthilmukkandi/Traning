define([
    "jquery"
], function($){
        "use strict";
        var mageJsComponent = function(config, node)
        {
            console.log(config);
            console.log(node);
        };

    return mageJsComponent;
    }
)